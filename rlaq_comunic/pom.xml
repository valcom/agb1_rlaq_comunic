<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>it.inps.entrate.rlaq</groupId>
    <artifactId>agb1_rlaq_comunic</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>
  <artifactId>rlaq_comunic</artifactId>
  
  <properties>
	  <srcContainer>rlaq_comunic</srcContainer>
	<staticContainer>${project.parent}/ag_rlaq_comunic</staticContainer>
	<baseDirectory>${project.parent.basedir}</baseDirectory>
  </properties>
  
  
  <build>
	 <plugins>
		 
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				  <version>3.5.0</version>

				<configuration>
					<descriptors>
						 <descriptor>${project.basedir}/src/main/resources/assembly.xml</descriptor>
					</descriptors>
					<outputDirectory>${project.parent.basedir}</outputDirectory>
					<finalName>ag_rlaq_comunic</finalName>
					<appendAssemblyId>false</appendAssemblyId>	
					<attach>false</attach>				
				</configuration>
				<executions>
					<execution>
						<id>create-archive</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
	 </plugins>
	 
 </build>
</project>